/* eslint-disable max-len, curly */
import * as path from 'path';
import * as vscode from 'vscode';

import { getLatestBuildArtifacts } from './buildArtifacts';
import { analyzeBuild, getLatestMemoryAnalysisReport, MemoryAnalyzerReport } from './memoryAnalyzer';

let currentPanel: vscode.WebviewPanel | undefined;

// 16px hierarchy icons adapted from the MIT-licensed reference analyzer.
const REGION_ICON = 'data:image/png;base64,';
const SECTION_ICON = 'data:image/png;base64,';
const SYMBOL_ICON = 'data:image/png;base64,';

function escapeHtml(value: string): string {
  return value.replace(/[&<>"']/g, (character) => {
    switch (character) {
      case '&': return '&amp;';
      case '<': return '&lt;';
      case '>': return '&gt;';
      case '"': return '&quot;';
      case "'": return '&#39;';
      default: return character;
    }
  });
}

function panelHtml(report: MemoryAnalyzerReport, nonce: string): string {
  const reportJson = JSON.stringify(report).replace(/</g, '\\u003c');
  return `<!DOCTYPE html><html lang="en"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width, initial-scale=1.0">
<meta http-equiv="Content-Security-Policy" content="default-src 'none'; img-src data:; style-src 'unsafe-inline'; script-src 'nonce-${nonce}';">
<style>
:root{color-scheme:light dark}body{font-family:var(--vscode-font-family);color:var(--vscode-editor-foreground);background:var(--vscode-editor-background);margin:0;padding:16px;height:100vh;overflow:hidden}.toolbar{display:flex;flex-wrap:wrap;align-items:center;gap:8px;margin-bottom:12px}.toolbar button{padding:6px 10px;border-radius:4px;border:1px solid var(--vscode-button-secondaryBorder,var(--vscode-button-border));background:var(--vscode-button-secondaryBackground);color:var(--vscode-button-secondaryForeground);cursor:pointer}.toolbar button:hover,.toolbar button.active{background:var(--vscode-button-secondaryHoverBackground)}.toolbar input[type=text]{min-width:220px;padding:6px 8px;border-radius:4px;border:1px solid var(--vscode-input-border);background:var(--vscode-input-background);color:var(--vscode-input-foreground)}.toolbar select{padding:6px 8px;border-radius:4px;border:1px solid var(--vscode-input-border);background:var(--vscode-input-background);color:var(--vscode-input-foreground)}.toolbar .toggle-button{font-size:12px;user-select:none}.search-count{font-size:12px;color:var(--vscode-descriptionForeground);margin-left:4px}.current-build{margin-bottom:12px;font-size:12px;color:var(--vscode-descriptionForeground)}.scroll-container{height:calc(100vh - 76px);overflow:auto}.analysis-table{background:var(--vscode-editor-background);color:var(--vscode-editor-foreground);width:100%;text-align:left;border-collapse:collapse}.analysis-table td,.analysis-table th{border:1px solid var(--vscode-editorWidget-border);padding:5px 8px}.analysis-table tbody td{font-size:13px}.analysis-table thead{background:var(--vscode-editorWidget-background);position:sticky;top:0;z-index:2}.analysis-table th{font-size:13px;font-weight:600}.analysis-table th.sortable{cursor:pointer;user-select:none}.analysis-table th.sortable:hover{background:var(--vscode-list-hoverBackground)}.analysis-table th.right,.analysis-table td.right{text-align:right}.analysis-table th.selection-header{width:58px;text-align:center;white-space:nowrap}.analysis-table td.selection-cell{text-align:center;width:58px}.analysis-table .toggle{cursor:pointer;display:inline-flex;width:22px;align-items:center;justify-content:center;user-select:none;color:var(--vscode-textLink-foreground);font-weight:600}.analysis-table .level-2 td:nth-child(3){padding-left:22px}.analysis-table .level-3 td:nth-child(3){padding-left:42px}.analysis-table .level-1{font-weight:600}.analysis-table .level-1 td:nth-child(3){font-size:14px}.analysis-table .level-2 td:nth-child(3){font-weight:500}.analysis-table tr:hover td{background:var(--vscode-list-hoverBackground)}.analysis-table tr.row-selected td{background:var(--vscode-list-activeSelectionBackground);color:var(--vscode-list-activeSelectionForeground)}.analysis-table .icon{display:inline-flex;width:16px;height:16px;margin-right:5px;vertical-align:middle;align-items:center;justify-content:center}.analysis-table .region-icon{color:var(--vscode-charts-blue)}.analysis-table .section-icon{color:var(--vscode-charts-orange)}.analysis-table .symbol-icon{color:var(--vscode-charts-green)}.analysis-table .usage-bar{display:inline-block;width:100px;height:1.4em;background:var(--vscode-editorWidget-border);vertical-align:middle;margin-right:8px}.analysis-table .usage-fill{display:block;height:100%;text-align:center;font-size:.85em;line-height:1.4em;min-width:2px}.analysis-table .source-link{color:var(--vscode-textLink-foreground);text-decoration:none;cursor:pointer}.analysis-table .source-link:hover{text-decoration:underline}.analysis-table .empty{text-align:center;color:var(--vscode-descriptionForeground);padding:18px}.clear-selection{font-size:11px;padding:1px 5px!important;margin-left:3px}.meta{color:var(--vscode-descriptionForeground);font-size:12px}
</style></head><body>
<div class="toolbar"><button id="collapseAll" type="button">Collapse all</button><button id="showSelected" class="toggle-button" type="button">Show selected</button><input id="searchInput" type="text" placeholder="Search symbols..."><button id="caseSensitive" class="toggle-button" type="button" title="Case sensitive search">Aa</button><span id="searchMatchCount" class="search-count"></span></div>
<div class="current-build"><strong>Build:</strong> ${escapeHtml(path.basename(report.elf))}<span class="meta"> · ${escapeHtml(report.profile || 'latest')}</span><br><strong>Map:</strong> ${escapeHtml(report.map || 'not found')}</div>
<div class="scroll-container"><table class="analysis-table"><thead><tr><th></th><th class="selection-header">Sel <button id="clearSelection" class="clear-selection" type="button" aria-label="Clear selection">✕</button></th><th class="sortable" data-sort="name">Name <span>⇅</span></th><th class="sortable" data-sort="address">Address <span>⇅</span></th><th class="sortable right" data-sort="size">Size <span>⇅</span></th><th class="right">Used</th><th class="right">Free</th></tr></thead><tbody id="analysisBody"></tbody></table></div>
<script nonce="${nonce}">const report=${reportJson};const icons={region:'${REGION_ICON}',section:'${SECTION_ICON}',symbol:'${SYMBOL_ICON}'};const vscode=acquireVsCodeApi();const body=document.getElementById('analysisBody');const searchInput=document.getElementById('searchInput');const caseSensitiveButton=document.getElementById('caseSensitive');const searchCount=document.getElementById('searchMatchCount');const selectedKeys=new Set();const expandedKeys=new Set();if(report.focusRegion)expandedKeys.add('region:'+report.focusRegion);let showSelectedOnly=false;let sortField=null;let sortAscending=true;const formatBytes=bytes=>{if(bytes<=0)return'0 B';const units=['B','KB','MB','GB'];const index=Math.min(Math.floor(Math.log(bytes)/Math.log(1024)),units.length-1);return parseFloat((bytes/Math.pow(1024,index)).toFixed(2))+' '+units[index]};const regionColor=percent=>percent>95?'var(--vscode-minimap-errorHighlight)':percent>75?'var(--vscode-minimap-warningHighlight)':'var(--vscode-minimap-infoHighlight)';const matchText=value=>{const query=searchInput.value.trim();if(!query)return true;const text=String(value||'');return(caseSensitiveButton.classList.contains('active')?text:text.toLowerCase()).includes(caseSensitiveButton.classList.contains('active')?query:query.toLowerCase())};const regionKey=region=>'region:'+region.name;const sectionKey=(region,section)=>'section:'+region.name+'::'+section.name;function cell(row,text,classes){const td=document.createElement('td');td.textContent=text;if(classes)td.className=classes;row.appendChild(td);return td}function iconCell(row,kind,text){const td=document.createElement('td');const icon=document.createElement('img');icon.className='icon '+kind+'-icon';icon.src=icons[kind];icon.alt='';td.appendChild(icon);td.appendChild(document.createTextNode(' '+text+' '));row.appendChild(td);return td}function addToggle(row,key,level,parentKey){const td=document.createElement('td');const toggle=document.createElement('span');toggle.className='toggle';toggle.textContent=expandedKeys.has(key)?'−':'+';toggle.onclick=event=>{event.stopPropagation();if(expandedKeys.has(key))expandedKeys.delete(key);else expandedKeys.add(key);render()};td.appendChild(toggle);row.appendChild(td);row.dataset.level=String(level);row.dataset.key=key;if(parentKey)row.dataset.parent=parentKey;return td}function addSymbolRow(region,section,symbol,index){const key=sectionKey(region,section)+'::'+symbol.name+'::'+symbol.address;const row=document.createElement('tr');row.className='level-3';row.dataset.key=key;row.style.display='none';row.appendChild(document.createElement('td'));row.dataset.level='3';row.dataset.parent=sectionKey(region,section);const select=cell(row,'','selection-cell');const checkbox=document.createElement('input');checkbox.type='checkbox';checkbox.checked=selectedKeys.has(key);checkbox.onchange=()=>{if(checkbox.checked)selectedKeys.add(key);else selectedKeys.delete(key);row.classList.toggle('row-selected',checkbox.checked)};select.appendChild(checkbox);const nameCell=iconCell(row,'symbol',symbol.name);if(symbol.source){const icon=nameCell.querySelector('img');if(icon)icon.alt='';const link=document.createElement('span');link.className='source-link';link.textContent=' '+symbol.name+' ';link.title=symbol.source;link.onclick=()=>vscode.postMessage({type:'openFile',file:symbol.sourceFile||symbol.source,line:symbol.sourceLine||1});nameCell.appendChild(link)}cell(row,'0x'+symbol.address.toString(16).padStart(8,'0'));cell(row,formatBytes(symbol.size),'right');cell(row,'','right');cell(row,'','right');return row}function render(){body.innerHTML='';let matches=0;report.regions.forEach(region=>{const percent=region.size?region.used/region.size*100:0;const regionMatch=matchText(region.name);const visibleSections=report.sections.filter(section=>section.region===region.name).map(section=>{const symbols=report.symbols.filter(symbol=>symbol.section===section.name&&symbol.region===region.name);const matchingSymbols=symbols.filter(symbol=>matchText(symbol.name)||matchText(symbol.source));const visible=(!showSelectedOnly||matchingSymbols.some(symbol=>selectedKeys.has(sectionKey(region,section)+'::'+symbol.name+'::'+symbol.address)))&&(regionMatch||matchText(section.name)||matchingSymbols.length>0);return {section,symbols,matchingSymbols,visible}}).filter(item=>item.visible);if(!regionMatch&&!visibleSections.length)return;if(searchInput.value.trim())matches+=visibleSections.reduce((total,item)=>total+item.matchingSymbols.length,0);const rKey=regionKey(region);const regionRow=document.createElement('tr');regionRow.className='level-1';regionRow.style.display='';addToggle(regionRow,rKey,1);cell(regionRow,'','selection-cell');const usage=iconCell(regionRow,'region',region.name);const bar=document.createElement('span');bar.className='usage-bar';const fill=document.createElement('span');fill.className='usage-fill';fill.style.width=Math.min(100,percent)+'%';fill.style.backgroundColor=regionColor(percent);fill.style.color=percent>50?'white':'black';fill.textContent=percent.toFixed(2)+'%';bar.appendChild(fill);usage.prepend(bar);cell(regionRow,'0x'+region.origin.toString(16).padStart(8,'0'));cell(regionRow,formatBytes(region.size),'right');cell(regionRow,formatBytes(region.used),'right');cell(regionRow,formatBytes(Math.max(0,region.size-region.used)),'right');body.appendChild(regionRow);const regionOpen=expandedKeys.has(rKey)||Boolean(searchInput.value.trim()&&visibleSections.length);visibleSections.forEach(item=>{const section=item.section;const sKey=sectionKey(region,section);const sectionRow=document.createElement('tr');sectionRow.className='level-2';sectionRow.style.display=regionOpen?'':'none';addToggle(sectionRow,sKey,2,rKey);cell(sectionRow,'','selection-cell');iconCell(sectionRow,'section',section.name);cell(sectionRow,'0x'+section.address.toString(16).padStart(8,'0'));cell(sectionRow,formatBytes(section.size),'right');cell(sectionRow,'','right');cell(sectionRow,'','right');body.appendChild(sectionRow);const sectionOpen=expandedKeys.has(sKey)||Boolean(searchInput.value.trim()&&item.matchingSymbols.length);const symbols=(sortField?item.symbols.slice().sort((a,b)=>{const left=sortField==='name'?a.name:sortField==='address'?a.address:a.size;const right=sortField==='name'?b.name:sortField==='address'?b.address:b.size;return(left<right?-1:left>right?1:0)*(sortAscending?1:-1)}):item.symbols);symbols.forEach((symbol,index)=>{const symbolRow=addSymbolRow(region,section,symbol,index);symbolRow.style.display=regionOpen&&sectionOpen?'':'none';body.appendChild(symbolRow)});});});searchCount.textContent=searchInput.value.trim()?(matches?'Found: '+matches+' symbols':'No matches'):'';body.querySelectorAll('tr.level-3').forEach(row=>{const checkbox=row.querySelector('input');if(checkbox?.checked)row.classList.add('row-selected')})}document.querySelectorAll('[data-sort]').forEach(header=>header.onclick=()=>{const field=header.dataset.sort;if(sortField===field){if(sortAscending)sortAscending=false;else{sortField=null;sortAscending=true}}else{sortField=field;sortAscending=true}render()});searchInput.oninput=render;caseSensitiveButton.onclick=()=>{caseSensitiveButton.classList.toggle('active');render()};document.getElementById('collapseAll').onclick=()=>{expandedKeys.clear();render()};document.getElementById('showSelected').onclick=()=>{showSelectedOnly=!showSelectedOnly;document.getElementById('showSelected').classList.toggle('active',showSelectedOnly);render()};document.getElementById('clearSelection').onclick=()=>{selectedKeys.clear();render()};render();</script></body></html>`;
}

async function findElf(): Promise<{elf: string; map?: string} | undefined> {
  const artifacts = getLatestBuildArtifacts();
  const root = vscode.workspace.workspaceFolders?.[0]?.uri.fsPath;
  if (artifacts.elf && root) return { elf: path.join(root, artifacts.elf), map: artifacts.map ? path.join(root, artifacts.map) : undefined };
  const files = await vscode.workspace.findFiles('**/*.elf', '**/{node_modules,.git}/**', 100);
  const elf = files[0]?.fsPath;
  if (!elf) return undefined;
  const map = path.join(path.dirname(elf), `${path.basename(elf, path.extname(elf))}.map`);
  try { await vscode.workspace.fs.stat(vscode.Uri.file(map)); return { elf, map }; } catch (error) { return { elf }; }
}

export async function openMemoryAnalyzer(focusRegion?: string): Promise<void> {
  const target = await findElf();
  if (!target) { await vscode.window.showInformationMessage('Build a project first so the memory analyzer can find an ELF file.'); return; }
  const toolchainPath = vscode.workspace.getConfiguration('stm32-for-vscode').get<string>('armToolchainPath', '');
  const cached = getLatestMemoryAnalysisReport();
  const report = cached && cached.elf === target.elf ? { ...cached } : await analyzeBuild(target.elf, target.map, toolchainPath);
  report.focusRegion = focusRegion;
  if (currentPanel) { currentPanel.reveal(vscode.ViewColumn.Active); currentPanel.webview.html = panelHtml(report, `${Date.now()}`); return; }
  currentPanel = vscode.window.createWebviewPanel('stm32MemoryAnalyzer', 'STM32 Memory Analyzer', vscode.ViewColumn.Active, { enableScripts: true, retainContextWhenHidden: true });
  currentPanel.webview.onDidReceiveMessage(async (message: {type?: string; file?: string; line?: number}) => { if (message.type !== 'openFile' || !message.file) return; const rootPath = vscode.workspace.workspaceFolders?.[0]?.uri.fsPath; const file = path.isAbsolute(message.file) ? message.file : path.join(rootPath || '', message.file); const document = await vscode.workspace.openTextDocument(vscode.Uri.file(file)); const line = Math.max(0, (message.line || 1) - 1); await vscode.window.showTextDocument(document, { selection: new vscode.Range(line, 0, line, 0), preview: false }); });
  currentPanel.onDidDispose(() => { currentPanel = undefined; });
  currentPanel.webview.html = panelHtml(report, `${Date.now()}`);
}

export async function refreshMemoryAnalyzer(elfPath: string, mapPath: string | undefined, toolchainPath: string | boolean): Promise<void> {
  if (!currentPanel) return;
  const cached = getLatestMemoryAnalysisReport();
  const report = cached && cached.elf === elfPath ? { ...cached } : await analyzeBuild(elfPath, mapPath, toolchainPath);
  currentPanel.webview.html = panelHtml(report, `${Date.now()}`);
}
